version: '3.9'

services:
  ieeg_recon:
    env_file:
      - dev.env
    image: nishantsinha89/ieeg_recon:latest
    volumes:
      - ./data:/data
    container_name: ieeg_recon
    build:
      context: .
      dockerfile: ./Dockerfile
